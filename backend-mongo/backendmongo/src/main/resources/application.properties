spring.application.name=backendmongo

server.port=${PORT:8080}

spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI}
spring.web.cors.enabled=false
# Logs para debugging en Railway
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.cors=DEBUG

security.jwt.secret=${JWT_SECRET:IronFit_2026_Sem5_JWT_Secret_Super_Largo_123456789}
security.jwt.expiration-minutes=${JWT_EXPIRATION_MINUTES:120}